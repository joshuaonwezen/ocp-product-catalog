# CLI — Validation

## Validate

Validates the app manifest and type-checks the TypeScript without uploading anything.

```bash
ocp app validate
```

Run this before `ocp app prepare` to catch manifest errors and type errors locally.
